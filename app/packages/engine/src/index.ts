/**
 * @ward-round/engine — the neutral discrete-event simulation core.
 *
 * This is a placeholder barrel for the §1 skeleton; the real engine
 * (RNG, heap, clock, scheduler, state, model, handlers) lands from §2 onward.
 * It carries no knowledge of Svelte, HTTP, HL7, money, or scoring.
 */
export const ENGINE_VERSION = "0.0.0";
