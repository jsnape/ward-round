/**
 * @ward-round/contract — the versioned business-event schema, translator, and
 * save format. The published, cross-language boundary.
 *
 * Placeholder barrel for the §1 skeleton; the TypeSpec schema, translator, sinks,
 * and save format land in §8 and §9. Depends only on the engine's public
 * domain-event types — never on Svelte, HTTP, or the web app.
 */
export const CONTRACT_VERSION = "0.0.0";
